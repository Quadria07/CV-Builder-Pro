{
    "version": 2,
    "buildCommand": "vite build",
    "outputDirectory": "dist",
    "framework": "vite",
    "functions": {
        "api/**/*.js": {
            "runtime": "@vercel/node@3",
            "maxDuration": 30
        }
    },
    "rewrites": [
        {
            "source": "/((?!api/).*)",
            "destination": "/index.html"
        }
    ]
}